---
:backends: - yaml
:logger: console
:hierarchy: - %{hostname}
            - env/%{env}
            - %{netgroup}
            - default
:yaml:
   :datadir: /etc/hieradata

:puppet:
   :datasource: data
